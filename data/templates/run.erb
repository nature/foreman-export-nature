#!/bin/sh
exec 2>&1

if [[ -e ~/.bash_profile ]]
then
  source ~/.bash_profile
fi

<% environment.each do |key, value| %>
export <%= key %>=<%= value%>
<% end %>

[[ $RVM_VERSION_STRING ]] && rvm use "$RVM_VERSION_STRING"

cd <%= execution_target %>
exec <%= command %>
